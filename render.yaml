services:
  - type: worker
    name: kindvoiceua
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false

      - key: GROQ_API_KEY
        sync: false